package concurrentcollections;

/**
 *
 * ConcurrentCollections: - >(if lot of reads and less writes)
 * //concurrently iteartes and modifies collection
 * ConcurrentHashMap
 * CopyOnWriteArrayList
 * CopyOnWriteArraySet.
 *
 * Synchronized: ->
 * HashTable
 * Vector
 *
 * Regular : -> (can be synchronized by using Collections class)
 * ArrayList
 * HashMap
 *
 */
public class ConcurrentCollection {
}
